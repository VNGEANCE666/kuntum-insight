# Pin versi persis dengan lingkungan Colab yang menghasilkan model,
# untuk menghindari perilaku prediksi yang berubah akibat perbedaan versi
# (lihat peringatan InconsistentVersionWarning saat loading di HANDOFF.md).
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
scikit-learn==1.6.1
joblib>=1.3
pandas>=2.0
numpy>=1.26
python-multipart>=0.0.9   # jika ada endpoint upload/form
